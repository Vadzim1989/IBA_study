/*
Написать функцию, у которой есть два параметра: строка и количество раз, которое нужно повторить строку.
Проверки на валидность передаваемых аргументов делать не надо.
*/

function repeatString(sString, iAmount) {
  return sString.repeat(iAmount);
}